// "How It Works" section. Owner: Person B.
// Elements: #viz-empty #viz-alphabet #viz-steps
// Called by encryptDecrypt.ts after a successful encrypt/decrypt.

// TODO: hide #viz-empty, render shifted-alphabet strip + per-letter table from shiftSteps()
export function renderVisualization(_text: string, _key: number, _mode: "encrypt" | "decrypt"): void {}
